# Supabase — read-only anon access to the canonical views.
# Copy to .env and fill in. NEVER commit .env.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key
